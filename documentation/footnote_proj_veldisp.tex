\paragraph{$^{$FOOTNOTE_NUMBER$}$The projected velocity dispersion} is computed along the projection axis. 
Along this axis, the velocity is a 1D quantity, so that the velocity dispersion is simply 1 value.
